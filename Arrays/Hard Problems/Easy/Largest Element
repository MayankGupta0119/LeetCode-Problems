#include <bits/stdc++.h> 
int largestElement(vector<int> &v, int n) {
    // Write your code here.
    int largest = v[0];
    for(int i = 0;i<n;i++){
        if(v[i] > largest){
            largest = v[i];
        }
    }
    return largest;
}
