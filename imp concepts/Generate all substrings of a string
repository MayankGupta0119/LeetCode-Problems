    void getsubstrings(vector<string> &v, string s){
      for(int i = 0;i<s.length();i++){
        for(int j = i+1;j<= s.length();j++){
          string ss = s.substr(i,j-i);
          v.push_back(ss);
        }
      }
    }
